// Construir um programa que leia três valores numéricos inteiros, representados pelas variáveis A, B e C em seguida realiza soma do resultado do quadrado de cada uma das variáveis, para calcular o quadrado devemos multiplicar um número por ele mesmo.

var A = 2;
var B = 4;
var C = 6;

var soma = (A ** 2) + (B ** 2) + (C ** 2);

console.log(soma);
